#!/bin/sh
# Pre-commit hook: ESLint only (tests run in CI)

echo "Running ESLint..."
npx eslint .
ESLINT_EXIT=$?

if [ $ESLINT_EXIT -ne 0 ]; then
  echo "ESLint failed. Fix errors before committing."
  exit 1
fi

echo "Pre-commit ESLint passed."
